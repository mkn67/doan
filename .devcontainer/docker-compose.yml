version: "3.9"

services:
  backend:
    build:
      context: ../backend
      dockerfile: ../.devcontainer/backend/Dockerfile
    container_name: spring_backend
    command: sleep infinity
    volumes:
      - ..:/workspaces/da:cached
      - maven_cache:/root/.m2
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ../frontend
      dockerfile: ../.devcontainer/frontend/Dockerfile
    container_name: react_frontend
    command: sleep infinity
    volumes:
      - ..:/workspaces/da:cached
    ports:
      - "3000:3000"

volumes:
  maven_cache: